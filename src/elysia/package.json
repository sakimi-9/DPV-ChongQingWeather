{
  "name": "app",
  "version": "1.0.50",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "prisma:generate": "bunx prisma generate",
    "prisma:push": "bunx prisma db push",
    "import:processed": "bun run src/scripts/import_processed_data.ts",
    "check:db": "bun run src/scripts/check_processed_tables.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "elysia": "latest",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prisma": "^6.6.0"
  },
  "module": "src/index.js"
}